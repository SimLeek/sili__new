[mypy]
strict = True
warn_unreachable = True
# Ratchet note: run `mypy sili` once, and if the legacy surface drowns you,
# add per-module overrides below instead of dropping strict globally.
exclude = (?x)(
    ^backburner/
    | ^docs/
    | ^tests/unit/python/   # stale legacy scripts
  )
